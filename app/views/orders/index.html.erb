<h1>Your Orders</h1>

<table class='table'>
  <thead>
    <tr>
      <th>Order number</th>
      <th>No. of Items</th>
      <th>Total</th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @orders.each do |order| %>
      <tr>
        <th>#<%=order.id%></th>
        <th><%=order.item_count%></th>
        <th><%=order.grand_total%></th>
        <td><%= link_to 'Show', user_order_path(user_id: current_user, id: order) %></td>
      </tr>
    <% end %>
  </tbody>
</table>
